<h1>Taco Time!</h1>

<h2>Tacos!</h2>

<ul>
  {{#each tacos}}
    {{#unless devoured}}
      {{> tacos/taco-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>We're out of these!</h2>

<ul>
  {{#each tacos}}
    {{#if devoured}}
      {{> tacos/taco-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Taco!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Taco Name:</label>
    <input type="text" id="taco" name="name">
  </div>

  <button type="submit">Add Taco!</button>
</form>
